#pragma once

/**
 * Event - Generic wrapper for an event that can be listened to.
 * ie. ControlEvent
 */
class Event {
    public:
        Event();
        ~Event();

    private:
};

Event::Event() {}

Event::~Event() {}