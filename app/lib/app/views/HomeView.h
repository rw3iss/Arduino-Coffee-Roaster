#pragma once

#include "View.h"

/**
 * HomeScreen - sets up a component stack for the default / HomeScreen.
 */
class HomeView : public View {
    public:
        HomeView() : View(){};
        ~HomeView(){};

    private:
};