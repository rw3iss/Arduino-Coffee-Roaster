#pragma once

/**
 * ViewRenderer - Handles drawing of a particular View to a Screen.
 * The View child heirarchy is laid out in a normal 1:1 ratio, and then scaled to the Screen dimensions, then drawn.
 */
class ViewRenderer {
   public:
    ViewRenderer();
    ~ViewRenderer();

   private:
};

ViewRenderer::ViewRenderer() {
}

ViewRenderer::~ViewRenderer() {
}